{
  "name": "vapi",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file='.env' src/app.ts",
    "server": "node --watch server/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && tsc",
    "execute": "npm run build && node --env-file='.env' dist/app.js",
    "prod": "npm run build && node --env-file='.env' dist/routes.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vapi-ai/server-sdk": "^0.10.2",
    "express": "^5.1.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.0",
    "rimraf": "^6.1.0"
  }
}
